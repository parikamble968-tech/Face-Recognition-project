# Face-Recognition-project
A face recognition project is a computer vision application that identifies or verifies individuals from images or video frames by analyzing unique facial patterns. The system captures a face, maps its unique landmarks, and matches them against a stored database to confirm identity for security, automation, or convenience.
